machine:
  pre:
    - sudo apt-get install -y tree
  node:
    version: 6

dependencies:
  pre:
    - git clone https://${GH_TOKEN}:x-oauth-basic@github.com/wsw0108/site-example $HOME/site-example

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh